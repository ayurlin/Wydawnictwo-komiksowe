<%= form_for(series) do |f| %>
  <% if series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= image_tag(series.icon, class: 'series-icon') %>
    <h4><%= series.name %></h4>
    <p>Dodaj logo serii</p>
    <%= f.file_field :icon %>
    <p>Pozycja na liście:</p>
    <%= f.number_field :position %>
    <p>Zmień opis serii:</p>
    <%= f.text_area :description %>
    <%= f.submit('Zapisz serię', class: 'btn btn-success') %>
<% end %>
